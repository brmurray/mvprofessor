from setuptools import setup

setup(name='mvprofessor',
      version='0.1',
      description='mapping the professor feeder',
      author='brmurray',
      license='',
      packages=['mvprofessor'],
      zip_safe=False)